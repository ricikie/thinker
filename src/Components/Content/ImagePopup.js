import React, { Fragment } from 'react';
import '../../resource/css/ImgPopup.css';

const ImgPopup = ({imageUrl, box}) => {
    console.log(imageUrl);
    console.log(box);
    return(
        <Fragment>

        </Fragment>
    );
}
export default ImgPopup;